package com.example;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePropertyResourceIT extends PropertyResourceTest {

    // Execute the same tests but in native mode.
}